syntax = "proto3";

option optimize_for = LITE_RUNTIME;

package tl.pro;

enum pk_cmd {
  none = 0;
  cmd_confirm = 100;
  cmd_verify = 101;

  cmd_player_max_value = 1000;
  cmd_player_float_value = 1001;
  cmd_player_bool_value = 1002;

  cmd_player_get_bag = 1100;
}

message packet {
  pk_cmd cmd = 1;
  bytes data = 2;
  int32 seq = 3;
}

message confirm {
  string message = 1;
  bool ok = 2;
  optional pk_cmd cmd = 3;
  optional bytes data = 4;
}